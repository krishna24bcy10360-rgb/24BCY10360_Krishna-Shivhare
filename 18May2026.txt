-- Basic verification commands
SHOW DATABASES;
USE VITCampus;
SHOW TABLES FROM VITCampus;

-- Check initial CSE table structure and data
SELECT * FROM cse;
DESC cse;

-- 1. ALTER OPERATIONS ON CSE TABLE
-- Adding new phone number columns
ALTER TABLE cse ADD (
    sphn BIGINT, 
    sphn2 BIGINT
);

-- Dropping one column as per lab task
ALTER TABLE cse DROP COLUMN sphn2;

-- Adding Country column with a DEFAULT value
ALTER TABLE cse ADD (
    sCountry VARCHAR(30) DEFAULT 'INDIA'
);

-- Renaming column from Scountry to SLocation
ALTER TABLE cse RENAME COLUMN sCountry TO SLocation;

-- Checking the updated structure of CSE table
SELECT * FROM cse;
DESC cse;

-- 2. WORKER TABLE OPERATIONS
-- Creating Worker Table
CREATE TABLE Worker (
    WORKER_ID INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    FIRST_NAME CHAR(25),
    LAST_NAME CHAR(25),
    SALARY INT,
    JOINING_DATE DATETIME,
    DEPARTMENT CHAR(25)
);

-- Inserting sample records into Worker table
INSERT INTO Worker (WORKER_ID, FIRST_NAME, LAST_NAME, SALARY, JOINING_DATE, DEPARTMENT) VALUES
(001, 'Monika', 'Arora', 100000, '2014-02-20 09:00:00', 'HR'),
(002, 'Niharika', 'Verma', 80000, '2014-06-11 09:00:00', 'Admin'),
(003, 'Vishal', 'Singhal', 300000, '2014-02-20 09:00:00', 'HR'),
(004, 'Amitabh', 'Singh', 500000, '2014-02-20 09:00:00', 'Admin'),
(005, 'Vivek', 'Bhati', 500000, '2014-06-11 09:00:00', 'Admin'),
(006, 'Vipul', 'Diwan', 200000, '2014-06-11 09:00:00', 'Account'),
(007, 'Satish', 'Kumar', 75000, '2014-01-20 09:00:00', 'Account'),
(008, 'Geetika', 'Chauhan', 90000, '2014-04-11 09:00:00', 'Admin');

-- View all workers before updates
SELECT * FROM worker;

-- Updating Worker 1 details
UPDATE worker SET FIRST_NAME='Jayanth', LAST_NAME='V' WHERE WORKER_ID=1;

-- Incrementing salary of all workers by 5000
UPDATE worker SET SALARY = SALARY + 5000; 

-- Deleting worker record with ID 8
DELETE FROM worker WHERE WORKER_ID=8;  

-- View final worker table state
SELECT * FROM worker;
